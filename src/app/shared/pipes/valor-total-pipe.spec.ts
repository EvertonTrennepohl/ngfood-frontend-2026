import { ValorTotalPipe } from './valor-total-pipe';

describe('ValorTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new ValorTotalPipe();
    expect(pipe).toBeTruthy();
  });
});

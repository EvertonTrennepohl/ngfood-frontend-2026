import { QuantidadeTotalPipe } from './quantidade-total-pipe';

describe('QuantidadeTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new QuantidadeTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
